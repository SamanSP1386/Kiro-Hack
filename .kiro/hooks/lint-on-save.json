{
  "name": "Lint on Save",
  "version": "1.0.0",
  "description": "Runs ESLint on TypeScript and TSX files whenever they are saved, so errors are caught immediately.",
  "when": {
    "type": "fileEdited",
    "patterns": ["src/**/*.ts", "src/**/*.tsx"]
  },
  "then": {
    "type": "runCommand",
    "command": "npm run lint"
  }
}
